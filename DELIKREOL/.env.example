# Supabase Configuration
# Get these from: https://supabase.com/dashboard/project/_/settings/api
VITE_SUPABASE_URL=https://<YOUR_PROJECT_REF>.supabase.co
VITE_SUPABASE_ANON_KEY=SUPABASE_ANON_KEY_REPLACE_ME
VITE_METRICS_WEBHOOK_URL=
VITE_SHEETS_ORDERS_URL=

# ERP Backend (local)
VITE_ERP_API_URL=http://localhost:3001
VITE_ERP_FALLBACK_DEMO=true
VITE_WHATSAPP_NUMBER=596696000000
VITE_CONTACT_EMAIL=contact@delikreol.mq
VITE_OPERATIONS_EMAIL=operations@delikreol.mq

# Free mode (no paid backend required)
VITE_FREE_MODE=false
VITE_ORDER_MODE=
VITE_ORDER_FORM_URL=

# Stripe Configuration (Client-side only)
# Get your publishable key from: https://dashboard.stripe.com/test/apikeys
# IMPORTANT: Never put your Stripe SECRET key here! It should only be in Supabase Edge Functions
VITE_STRIPE_PUBLISHABLE_KEY=STRIPE_PUBLISHABLE_KEY_REPLACE_ME

# Application Configuration (Optional)
VITE_APP_NAME=Delikreol
VITE_APP_ENV=development

# Notes:
# - All variables prefixed with VITE_ are exposed to the browser
# - Never commit your actual .env file with real credentials
#
# API Keys Security - IMPORTANT:
# The following API keys should NEVER be in this file or any frontend code!
# They must be configured in Supabase Dashboard > Project Settings > Edge Functions > Secrets:
#
#   STRIPE_SECRET_KEY=STRIPE_SECRET_KEY_REPLACE_ME
#
# For OpenAI, Meta, and Google Sheets integrations:
# - These keys are stored securely in the Supabase database (api_keys table)
# - They are managed through the Admin Panel (APIKeysManager component)
# - Only administrators can view and configure these keys
# - The keys are used server-side via Edge Functions only
#
# Never expose API keys in:
# - Environment variables prefixed with VITE_
# - Client-side code
# - Git repositories
# - Public documentation
